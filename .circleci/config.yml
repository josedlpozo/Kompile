version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7.10
    working_directory: ~/kompile

    steps:
      - checkout
      - run: npm install
      - run: npm install --dev
      - run: npm test
