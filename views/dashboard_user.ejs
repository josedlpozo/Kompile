<!DOCTYPE html>
<html>
  <head>
    <title>Kompile</title>
  </head>
  <body>
    <h1>Kompile</h1>
    <p>Here are your compile times</p>
    <th>User <%= user.email %></th>

    <table>
      <tr>
        <th>id</th>
        <th>Duration</th>
      </tr>
      <% for (var i = 0; i < kompiles.length; i++) { %>
        <tr>
          <td><%= kompiles[i].id %></td>
          <td><%= kompiles[i].duration %></td>
        </tr>
    <% } %>
    </table>

    <% if (user) { %>
    <form action="/logout" method="post">
        <button type="submit" class="btn btn-warning btn-lg">Logout</button>
    </form>
    <% } %>
  </body>
</html>